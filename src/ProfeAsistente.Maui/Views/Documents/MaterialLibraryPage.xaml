<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:ProfeAsistente.Maui.ViewModels.Documents"
             xmlns:dto="clr-namespace:ProfeAsistente.Shared.Dtos;assembly=ProfeAsistente.Shared"
             x:Class="ProfeAsistente.Maui.Views.Documents.MaterialLibraryPage"
             x:DataType="vm:MaterialLibraryViewModel"
             Title="Biblioteca">

    <Grid RowDefinitions="Auto,*" Padding="20" RowSpacing="12">
        <VerticalStackLayout Spacing="10">
            <Label Text="{Binding TituloPagina}" Style="{StaticResource TitleLabel}"/>
            <Label Text="Guías, actividades y pruebas de sus clases." Style="{StaticResource CaptionLabel}"/>
            <Label Text="{Binding MensajeEstado}" Style="{StaticResource CaptionLabel}"/>
            <Button Text="Ver todos los cursos" Command="{Binding LimpiarFiltroCursoCommand}"
                    Style="{StaticResource GhostButton}" IsVisible="{Binding FiltroCursoActivo}"/>

            <Entry Text="{Binding Busqueda}" Placeholder="Buscar por título, OA o curso"
                   ReturnCommand="{Binding BuscarCommand}"/>
            <Label Text="Tipo" FontSize="13" FontAttributes="Bold"/>
            <CollectionView ItemsSource="{Binding FiltrosTipo}" SelectedItem="{Binding FiltroTipo}"
                            SelectionMode="Single" HeightRequest="48">
                <CollectionView.ItemsLayout>
                    <LinearItemsLayout Orientation="Horizontal" ItemSpacing="8"/>
                </CollectionView.ItemsLayout>
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Border StrokeThickness="1" Stroke="#CCC" Padding="12,8"
                                StrokeShape="RoundRectangle 8" BackgroundColor="#F7F7F7">
                            <Label Text="{Binding .}" VerticalOptions="Center"/>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
            <Button Text="Actualizar" Command="{Binding CargarCommand}" Style="{StaticResource GhostButton}"/>
        </VerticalStackLayout>

        <CollectionView Grid.Row="1" ItemsSource="{Binding Materiales}" SelectionMode="None">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="dto:EducationalDocumentSummaryDto">
                    <Border Style="{StaticResource Card}" Margin="0,0,0,10">
                        <Border.GestureRecognizers>
                            <TapGestureRecognizer
                                Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.AbrirCommand}"
                                CommandParameter="{Binding .}"/>
                        </Border.GestureRecognizers>
                        <VerticalStackLayout Spacing="4">
                            <HorizontalStackLayout Spacing="8">
                                <Label Text="{Binding TypeLabel}" FontAttributes="Bold"
                                       TextColor="{StaticResource Primary}"/>
                                <Label Text="{Binding StatusLabel}" Style="{StaticResource CaptionLabel}"
                                       VerticalOptions="Center"/>
                            </HorizontalStackLayout>
                            <Label Text="{Binding Title}" FontAttributes="Bold" LineBreakMode="WordWrap"/>
                            <Label Text="{Binding ContextLine}" Style="{StaticResource CaptionLabel}" LineBreakMode="WordWrap"/>
                            <Label FontSize="12" TextColor="#666"
                                   Text="{Binding ItemCount, StringFormat='{}{0} ítems'}"/>
                            <Label Text="Desactualizado respecto de la clase" TextColor="#E65100"
                                   FontSize="12" FontAttributes="Bold" IsVisible="{Binding IsOutdated}"/>
                            <Button Text="Ir a la clase" Style="{StaticResource GhostButton}"
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.AbrirClaseCommand}"
                                    CommandParameter="{Binding .}"/>
                        </VerticalStackLayout>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <ActivityIndicator Grid.Row="1" IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}"
                           VerticalOptions="Center" HorizontalOptions="Center"/>
    </Grid>
</ContentPage>
